[package]
authors = ["BlackDex <black.dex@gmail.com>"]
name = "mimalloccrate"
version = "0.2.1"
edition = "2021"
resolver = "2"

[dependencies]
rocket = "0.5.0-rc.1"

# mimalloc = { version = "*", features = ["secure"] }
# mimalloc = { version = "*", features = ["secure", "submodule_update"] }

# mimalloc = { version = "*", features = ["v1_dev", "secure"] }
mimalloc = { version = "*", features = ["v1_dev", "secure", "submodule_update"] }

# mimalloc = { version = "*", features = ["v2_dev", "secure"] }
# mimalloc = { version = "*", features = ["v2_dev", "secure", "submodule_update"] }

[patch.crates-io]
# Use mimalloc with v1 dev and v2 support
mimalloc = { git = 'https://github.com/BlackDex/mimalloc_rust', branch = 'add-dev-repo-support' }
